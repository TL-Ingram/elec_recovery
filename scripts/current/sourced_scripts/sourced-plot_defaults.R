# ------------------------------------------------------------------------------
#####
# ggplot defaults
plot_defaults <- list(
  theme_bw() +
    theme(axis.text.x = element_text(angle = 30, vjust = 0.5, 
                                                            hjust=0.5))
)

plot_defaults_two <- list(
  theme_bw() +
    theme(axis.text.x = element_text(angle = 30, vjust = 0.5, 
                                                            hjust=0.5))
)
# ------------------------------------------------------------------------------